// str :: "hello world\n\0" // TODO: auto add null??
/*
phi :: proc(n: int) {
  acc: float = 2
  for {
    next := 1 + pow(acc, 1/n)
    if next == acc {ret next}
    acc = next
  }
}

getRoot :: proc(node: ^T) {
  for node.parent {
    node = node.parent
  }
  ret node
}
*/
